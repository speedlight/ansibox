version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3
    steps:
      - checkout
      - run:
          name: Install systems dependencies
          command: apt-get update && apt-get install -y python-pip

      - run:
          name: Install aws-cli and ansible
          command: pip install --upgrade awscli ansible

workflows:
    version: 2
    build-deploy:
        jobs:
            - build
